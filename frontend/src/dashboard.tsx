import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "lesson tracker" />
      <CardContent> An app to help parents track their children after school lessons/activities attendance < /CardContent>
      </Card>
    );