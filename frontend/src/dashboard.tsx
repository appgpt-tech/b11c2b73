import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "studytracker" />
      <CardContent> An application to help parents track their children's attendance in various after school lessons. < /CardContent>
      </Card>
    );