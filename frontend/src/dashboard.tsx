import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "lessontracker" />
      <CardContent> Helps parents track their children's after school lessons/activities attendance < /CardContent>
      </Card>
    );