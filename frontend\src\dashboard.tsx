import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "picatchu" />
      <CardContent> A web app to track my collection of Pokemon collectible cards < /CardContent>
      </Card>
    );